spring:
  application:
    name: spring-webflux-tutorial
  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:dev-only-jwt-secret-please-override-in-production-0123456789}
  expiration-ms: 3600000
